# ContextForge — Dependencies
# Install everything with: pip install -r requirements.txt

# HTTP server & routing
flask>=3.0.0

# LLM backends
openai>=1.0.0
anthropic>=0.25.0

# HTTP client (for Grok / custom endpoints)
httpx>=0.27.0

# Schema validation
jsonschema>=4.22.0

# Environment variable management
python-dotenv>=1.0.0

# Audio playback (TTS output)
pygame>=2.5.0

# Audio recording (STT input)
sounddevice>=0.4.6
soundfile>=0.12.1

# Optional — local Whisper STT (no API cost, ~140MB model download on first run)
# Uncomment if using stt_backend: whisper_local
# openai-whisper

# Optional — local TTS (no API cost, sounds robotic)
# Uncomment if using tts_backend: pyttsx3
# pyttsx3
